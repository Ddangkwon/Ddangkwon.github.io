---
layout : post
title : 이진 탐색 연습 문제
comments : true
categories : 
- CodingTest
tags : [CodingTest, Binary Search]
---

# 이진 탐색 연습 문제

## 문제

N개의 정렬된 데이터가 주어지고 Q개의 질의가 주어진다.

정렬된 데이터에서 목표값이 있는 위치(인덱스)를 찾는 프로그램을 작성하시오.


## 풀이

```cpp
#include <iostream>

#define ARR_MAX_LEN 500001
using namespace std;

int N, Q;
int arr[ARR_MAX_LEN] = { 0 };


int binary_search(int num)
{
    int low = 0, high = N - 1, mid = (low + high) / 2;

    while(low <= high){
        if(arr[mid] == num)
            return mid;   
        else if(arr[mid] < num)
            low = mid + 1;
        else
            high = mid - 1;

        mid = (low + high) / 2;
    }


    return -1;
}
int main(void)
{
    
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    cin >> N;

    for(int i = 0; i < N; i++){
        cin >> arr[i];
    }

    cin >> Q;

    for(int i = 0; i < Q; i++){
        int answer = 0, search_num;

        cin >> search_num;
        answer = binary_search(search_num);

        cout << answer << " ";
    }
    cout << "\n";


    return 0;
}
```