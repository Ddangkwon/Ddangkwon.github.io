---
layout : post
title : 감시
comments : true
categories : 
- CodingTest
tags : [CodingTest, simulation]

---

# 감시

http://colorful-palette.tistory.com/93

```cpp
#include <iostream>
#include <vector>
#define ARR_MAX_LEN 15
using namespace std;
int N, M, answer = 7654321;
int arrmap[ARR_MAX_LEN][ARR_MAX_LEN] = { 0 };


typedef struct{
    int y;
    int x;
    int num;
}cctv;
  
// 상하 좌우
int dy[4] = {-1, 1, 0, 0};
int dx[4] = {0, 0, -1, 1};
vector<cctv> cctv_lst;
vector<vector<int> > cctv_dir[6]{
    {{}}
    {{0}, {1}, {2}, {3}},
    {{2, 3}, {0, 1}},
    {{0, 3}, {1, 3}, {1, 2}, {0, 2}},
    {{0, 2, 3}, {0, 1, 3}, {1, 2, 3}, {0, 1, 2}},
    {{0, 1, 2, 3}},
};

void input_func(){

    cin >> N >> M;

    for(int i = 0; i < N; i++){
        for(int j = 0; j < M; j++){
            cin >> arrmap[i][j];
            if(arrmap[i][j] >= 1 && arrmap[i][j] <= 5)
                cctv_lst.push_back({i, j, arrmap[i][j]})
        }
    }
}

void find_min_blind_spot_func(){
    int temp_arr[N][M] = { 0 };

}


int main(){

    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    input_func();
    find_min_blind_spot_func();
    return 0;
}
```

