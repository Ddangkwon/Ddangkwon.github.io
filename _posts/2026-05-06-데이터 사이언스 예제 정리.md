---
layout : post
title : 데이터 사이언스 예제 정리
comments : true
categories :
- Data Sceince
tags : [Data Sceince]
---
# 데이터 사이언스 예제 정리

## 1. 데이터의 기본 단위

### 문제 1. 모집단과 표본 구분

전국 모든 고등학생의 평균 키를 알고 싶지만, 현실적으로 전체 조사가 어려워 1,000명의 학생만 뽑아 키를 조사했다.
이때 모집단과 표본의 연결로 가장 적절한 것은 무엇인가?

① 모집단: 1,000명의 학생, 표본: 전국 모든 고등학생
② 모집단: 전국 모든 고등학생, 표본: 조사한 1,000명의 학생
③ 모집단: 평균 키, 표본: 키를 재는 행위
④ 모집단: 키가 큰 학생, 표본: 키가 작은 학생

#### 정답

②

#### 해설

모집단(Population)은 분석자가 알고 싶은 전체 대상이다. 여기서는 전국 모든 고등학생이 모집단이다.
표본(Sample)은 모집단에서 일부만 뽑은 데이터이다. 여기서는 실제로 조사한 1,000명의 학생이 표본이다.

---

### 문제 2. 모수와 통계량

어떤 공장에서 생산되는 전체 배터리의 실제 평균 수명이 500시간이라고 하자. 이 중 100개를 뽑아 계산한 평균 수명은 492시간이었다.
이때 모수(Parameter)와 통계량(Statistic)의 연결로 가장 적절한 것은 무엇인가?

① 모수: 492시간, 통계량: 500시간
② 모수: 500시간, 통계량: 492시간
③ 모수: 100개, 통계량: 전체 배터리
④ 모수와 통계량은 모두 500시간

#### 정답

②

#### 해설

모수(Parameter)는 모집단의 실제 특성값이다. 따라서 전체 배터리의 실제 평균 수명 500시간이 모수이다.
통계량(Statistic)은 표본에서 계산한 값이다. 따라서 100개 표본에서 계산한 평균 492시간이 통계량이다.

---

### 문제 3. 독립변수와 종속변수

RF 품질 데이터를 이용해 Throughput을 예측하려고 한다. 입력 데이터는 RSRP(Reference Signal Received Power), RSRQ(Reference Signal Received Quality), SINR(Signal to Interference plus Noise Ratio), Band 정보이다.
이때 종속변수(Dependent Variable)는 무엇인가?

① RSRP(Reference Signal Received Power)
② RSRQ(Reference Signal Received Quality)
③ SINR(Signal to Interference plus Noise Ratio)
④ Throughput

#### 정답

④

#### 해설

독립변수(Independent Variable)는 결과를 설명하거나 예측하는 입력 변수이다.
종속변수(Dependent Variable)는 예측하려는 결과 변수이다.
이 문제에서는 RSRP, RSRQ, SINR, Band를 사용해 Throughput을 예측하므로 Throughput이 종속변수이다.

---

## 2. 기초 통계량

### 문제 4. 평균과 중앙값

다음 데이터가 있다.

1, 2, 3, 4, 100

이 데이터에서 평균과 중앙값으로 올바른 것은 무엇인가?

① 평균 22, 중앙값 3
② 평균 3, 중앙값 22
③ 평균 100, 중앙값 4
④ 평균 4, 중앙값 100

#### 정답

①

#### 해설

평균은 모든 값을 더한 뒤 데이터 개수로 나눈 값이다.

$$
\frac{1 + 2 + 3 + 4 + 100}{5} = \frac{110}{5} = 22
$$

중앙값(Median)은 데이터를 정렬했을 때 가운데 위치한 값이다.
정렬된 데이터는 이미 1, 2, 3, 4, 100이고 가운데 값은 3이다.
이 예시는 평균이 이상치(Outlier)에 민감하고, 중앙값은 이상치에 상대적으로 강하다는 점을 보여준다.

---

### 문제 5. 최빈값

다음 데이터의 최빈값(Mode)은 무엇인가?

2, 3, 3, 5, 5, 5, 7, 8

① 3
② 5
③ 7
④ 8

#### 정답

②

#### 해설

최빈값(Mode)은 가장 자주 등장하는 값이다.
3은 2번, 5는 3번 등장한다. 따라서 최빈값은 5이다.
최빈값은 범주형 데이터의 대표값을 볼 때도 유용하다.

---

### 문제 6. 분산과 표준편차의 해석

다음 중 표준편차(Standard Deviation)에 대한 설명으로 가장 적절한 것은 무엇인가?

① 데이터가 평균에서 얼마나 퍼져 있는지를 원래 단위로 해석할 수 있게 한다.
② 항상 평균보다 크다.
③ 범주형 데이터에서만 사용한다.
④ 데이터 개수가 많아지면 반드시 0이 된다.

#### 정답

①

#### 해설

분산(Variance)은 편차를 제곱해 평균적인 퍼짐을 측정한다. 하지만 단위가 원래 단위의 제곱이 되어 해석이 어렵다.
표준편차는 분산에 제곱근을 취한 값이므로 원래 단위로 해석할 수 있다.
예를 들어 점수 데이터의 표준편차는 점수 단위로 해석된다.

---

### 문제 7. 표본분산 계산

표본 데이터가 2, 4, 6이라고 하자.
표본분산을 구하라.

① 2
② 4
③ 8
④ 16

#### 정답

②

#### 해설

표본 평균은 다음과 같다.

$$
\bar{x} = \frac{2 + 4 + 6}{3} = 4
$$

각 값의 편차 제곱은 다음과 같다.

$$
(2 - 4)^2 = 4
$$

$$
(4 - 4)^2 = 0
$$

$$
(6 - 4)^2 = 4
$$

표본분산은 편차 제곱합을 n - 1로 나눈다.

$$
s^2 = \frac{4 + 0 + 4}{3 - 1} = \frac{8}{2} = 4
$$

따라서 정답은 4이다.

---

## 3. 확률과 확률분포

### 문제 8. 확률의 세 가지 공리

다음 중 확률의 공리로 보기 어려운 것은 무엇인가?

① 모든 사건의 확률은 0 이상이다.
② 표본공간 전체의 확률은 1이다.
③ 서로 겹치지 않는 사건들의 합집합 확률은 각 확률의 합이다.
④ 모든 사건의 확률은 반드시 0.5이다.

#### 정답

④

#### 해설

확률의 세 가지 공리는 비음수성, 전체확률 1, 가산성이다.
모든 사건의 확률이 반드시 0.5일 필요는 없다. 확률은 0에서 1 사이의 값을 가질 수 있다.

---

### 문제 9. 조건부확률 계산

전체 단말 로그 1,000개 중 Call Drop이 발생한 로그가 80개이다.
그중 RSRP(Reference Signal Received Power)가 나쁜 로그는 60개이다.
Call Drop이 발생했다는 조건에서 RSRP가 나쁠 확률은 얼마인가?

① 0.06
② 0.08
③ 0.60
④ 0.75

#### 정답

④

#### 해설

조건부확률(Conditional Probability)은 조건이 주어진 집단 안에서 사건이 발생한 비율이다.
여기서는 Call Drop이 발생한 80개 로그가 기준 집단이다.
그중 RSRP가 나쁜 로그는 60개이다.

$$
P(\text{RSRP 나쁨 given Call Drop}) = \frac{60}{80} = 0.75
$$

따라서 정답은 0.75이다.

---

### 문제 10. 독립 사건

두 사건 A와 B에 대해 다음이 성립한다.

$$
P(A) = 0.4
$$

$$
P(B) = 0.5
$$

$$
P(A \cap B) = 0.2
$$

A와 B는 독립인가?

① 독립이다.
② 독립이 아니다.
③ 상호배반이다.
④ 판단할 수 없다.

#### 정답

①

#### 해설

두 사건이 독립이면 다음이 성립한다.

$$
P(A \cap B) = P(A) \times P(B)
$$

계산하면 다음과 같다.

$$
0.4 \times 0.5 = 0.2
$$

주어진 교집합 확률도 0.2이므로 A와 B는 독립이다.

---

### 문제 11. 상호배반과 독립의 차이

확률이 0이 아닌 두 사건 A와 B가 상호배반(Mutually Exclusive)이라고 하자.
다음 중 올바른 설명은 무엇인가?

① 두 사건은 반드시 독립이다.
② 두 사건은 동시에 발생할 수 없다.
③ 두 사건의 교집합 확률은 항상 1이다.
④ 두 사건의 조건부확률은 항상 1이다.

#### 정답

②

#### 해설

상호배반은 두 사건이 동시에 발생할 수 없다는 뜻이다.
따라서 다음이 성립한다.

$$
P(A \cap B) = 0
$$

확률이 0이 아닌 두 사건이 상호배반이면 일반적으로 독립이 아니다.
독립은 한 사건 발생이 다른 사건의 확률에 영향을 주지 않는다는 뜻이다.

---

### 문제 12. 이산분포와 연속분포

다음 중 이산분포(Discrete Distribution)를 사용하는 상황으로 가장 적절한 것은 무엇인가?

① 하루 동안 발생한 장애 횟수
② 사람의 키
③ 신호 세기 측정값
④ 배터리 방전까지 걸린 시간

#### 정답

①

#### 해설

이산분포는 셀 수 있는 값을 가지는 확률변수에 사용한다.
장애 횟수는 0회, 1회, 2회처럼 셀 수 있는 개수 데이터이다.
사람의 키, 신호 세기, 시간은 연속적인 측정값이므로 연속분포를 사용하는 것이 더 적절하다.

---

## 4. 정규분포, z-score, 스케일링

### 문제 13. 정규분포 특징

다음 중 정규분포(Normal Distribution)의 특징으로 올바르지 않은 것은 무엇인가?

① 평균을 중심으로 좌우 대칭이다.
② 완전한 정규분포에서는 평균, 중앙값, 최빈값이 같다.
③ 평균과 표준편차로 모양이 결정된다.
④ 항상 오른쪽으로 긴 꼬리를 가진다.

#### 정답

④

#### 해설

정규분포는 평균을 중심으로 좌우 대칭인 종 모양의 연속확률분포이다.
항상 오른쪽으로 긴 꼬리를 가지는 것은 아니다. 오른쪽 꼬리가 긴 분포는 양의 왜도가 있는 비대칭 분포이다.

---

### 문제 14. z-score 계산

시험 점수가 평균 70점, 표준편차 10점인 정규분포를 따른다고 하자.
어떤 학생이 85점을 받았다면 z-score는 얼마인가?

① 0.5
② 1.0
③ 1.5
④ 2.0

#### 정답

③

#### 해설

z-score는 특정 값이 평균으로부터 표준편차 몇 배만큼 떨어져 있는지를 나타낸다.

$$
z = \frac{x - \mu}{\sigma}
$$

값을 대입하면 다음과 같다.

$$
z = \frac{85 - 70}{10} = 1.5
$$

즉 85점은 평균보다 표준편차 1.5배 높은 위치에 있다.

---

### 문제 15. 표준화와 정규화

다음 중 표준화(Standardization)에 대한 설명으로 가장 적절한 것은 무엇인가?

① 값을 0에서 1 사이로 바꾼다.
② 데이터를 평균 0, 표준편차 1이 되도록 변환한다.
③ 모든 데이터를 정규분포로 강제로 만든다.
④ 결측치를 제거하는 방법이다.

#### 정답

②

#### 해설

표준화(Standardization)는 데이터를 평균 0, 표준편차 1이 되도록 변환하는 방법이다.
값을 0에서 1 사이로 바꾸는 것은 보통 정규화(Normalization), 특히 Min-Max Scaling에 해당한다.
정규분포화는 데이터의 분포를 정규분포에 가깝게 만드는 작업이므로 표준화와 다르다.

---

### 문제 16. 중심극한정리

중심극한정리(Central Limit Theorem)에 대한 설명으로 가장 적절한 것은 무엇인가?

① 모집단이 정규분포가 아니면 표본평균도 절대 정규분포에 가까워질 수 없다.
② 표본 크기가 충분히 크면 표본평균의 분포가 정규분포에 가까워진다.
③ 표본 크기가 커질수록 데이터 원본이 반드시 정규분포가 된다.
④ 모든 표본은 반드시 같은 평균을 가진다.

#### 정답

②

#### 해설

중심극한정리(Central Limit Theorem)는 모집단 분포가 정규분포가 아니더라도, 표본 크기가 충분히 크면 표본평균의 분포가 정규분포에 가까워진다는 정리이다.
이 정리는 신뢰구간과 가설검정의 중요한 이론적 기반이다.

---

## 5. 상관, 공분산, 인과

### 문제 17. 공분산 해석

두 변수 X와 Y의 공분산이 양수라고 하자. 가장 적절한 해석은 무엇인가?

① X가 증가할 때 Y도 증가하는 경향이 있다.
② X가 증가할 때 Y는 반드시 감소한다.
③ X와 Y는 반드시 독립이다.
④ X와 Y는 반드시 인과관계이다.

#### 정답

①

#### 해설

공분산(Covariance)은 두 변수가 함께 움직이는 방향을 나타낸다.
공분산이 양수이면 두 변수가 같은 방향으로 움직이는 경향이 있다.
다만 공분산의 크기는 단위에 영향을 받기 때문에 크기 자체를 직접 비교하기 어렵다.

---

### 문제 18. 피어슨 상관계수

피어슨 상관계수(Pearson Correlation Coefficient)가 -0.92로 나왔다.
가장 적절한 해석은 무엇인가?

① 두 변수는 강한 양의 선형 관계를 가진다.
② 두 변수는 강한 음의 선형 관계를 가진다.
③ 두 변수는 아무 관계도 없다.
④ 두 변수는 반드시 인과관계이다.

#### 정답

②

#### 해설

피어슨 상관계수는 -1에서 1 사이 값을 가진다.
-1에 가까울수록 강한 음의 선형 관계이다.
따라서 -0.92는 한 변수가 증가할 때 다른 변수가 감소하는 강한 선형 관계로 해석할 수 있다.
하지만 상관은 인과를 의미하지 않는다.

---

### 문제 19. 스피어만 순위상관

다음 중 스피어만 순위상관(Spearman Rank Correlation)을 사용하는 상황으로 가장 적절한 것은 무엇인가?

① 두 변수의 정확한 단위 크기보다 순위 관계가 중요할 때
② 두 변수의 평균 차이를 비교할 때
③ 범주형 변수의 독립성을 검정할 때
④ 세 집단 이상의 평균 차이를 비교할 때

#### 정답

①

#### 해설

스피어만 순위상관은 실제 값 대신 순위를 이용해 관계를 측정한다.
이상치에 덜 민감하고, 직선이 아니더라도 단조 증가 또는 단조 감소 관계를 포착할 수 있다.

---

### 문제 20. 상관과 인과

아이스크림 판매량과 익사 사고 수가 여름에 함께 증가했다.
이 결과만 보고 “아이스크림이 익사 사고를 일으킨다”고 결론 내릴 수 없는 이유는 무엇인가?

① 두 변수 사이에 상관이 전혀 없기 때문이다.
② 상관이 높아도 숨은 변수나 다른 원인이 있을 수 있기 때문이다.
③ 상관계수는 항상 0이기 때문이다.
④ 두 변수는 반드시 독립이기 때문이다.

#### 정답

②

#### 해설

상관(Correlation)이 높다고 해서 인과(Causation)가 있다는 뜻은 아니다.
아이스크림 판매량과 익사 사고 수는 모두 여름이라는 숨은 변수의 영향을 받을 수 있다.
인과를 확인하려면 무작위 실험, A/B Test, 통제 변수, 도메인 지식 등이 필요하다.

---

## 6. 가설검정과 신뢰구간

### 문제 21. p-value 해석

어떤 검정에서 p-value가 0.03으로 나왔다.
이에 대한 올바른 해석은 무엇인가?

① 귀무가설이 참일 확률이 3%이다.
② 귀무가설이 거짓일 확률이 97%이다.
③ 귀무가설이 참이라고 가정했을 때 현재 결과만큼 또는 더 극단적인 결과가 나올 확률이 3%이다.
④ 대립가설이 반드시 참이다.

#### 정답

③

#### 해설

p-value는 귀무가설이 참이라고 가정했을 때, 현재 관측된 결과 또는 그보다 더 극단적인 결과가 나올 확률이다.
p-value가 귀무가설이 참일 확률이라는 해석은 잘못된 해석이다.

---

### 문제 22. 유의수준과 판단

유의수준(Significance Level)을 0.05로 정했고, 검정 결과 p-value가 0.08이었다.
올바른 판단은 무엇인가?

① 귀무가설을 기각한다.
② 귀무가설을 기각하지 못한다.
③ 대립가설이 반드시 참이다.
④ 귀무가설이 반드시 참이다.

#### 정답

②

#### 해설

가설검정에서는 p-value와 유의수준을 비교한다.

$$
p\text{-value} = 0.08
$$

$$
\alpha = 0.05
$$

p-value가 유의수준보다 크므로 귀무가설을 기각하지 못한다.
이것은 귀무가설이 참이라는 뜻이 아니라, 현재 데이터로는 귀무가설을 기각할 충분한 근거가 없다는 뜻이다.

---

### 문제 23. 1종 오류와 2종 오류

실제로는 신약 효과가 없는데, 표본 결과만 보고 효과가 있다고 판단했다.
이 오류는 무엇인가?

① 1종 오류(Type I Error)
② 2종 오류(Type II Error)
③ 검정력(Power)
④ 표준오차(Standard Error)

#### 정답

①

#### 해설

1종 오류(Type I Error)는 귀무가설이 참인데 귀무가설을 기각하는 오류이다.
신약 효과가 없다는 귀무가설이 실제로 참인데 효과가 있다고 판단했으므로 1종 오류이다.
2종 오류(Type II Error)는 실제 효과가 있는데도 효과를 발견하지 못하는 오류이다.

---

### 문제 24. 검정력

다음 중 검정력(Power)에 대한 설명으로 가장 적절한 것은 무엇인가?

① 실제 효과가 있을 때 귀무가설을 올바르게 기각할 확률이다.
② 귀무가설이 참일 때 귀무가설을 잘못 기각할 확률이다.
③ 표본 평균과 모평균의 차이이다.
④ p-value와 항상 같다.

#### 정답

①

#### 해설

검정력(Power)은 실제 효과가 있을 때 귀무가설을 올바르게 기각할 확률이다.
검정력이 높으면 실제 효과를 놓칠 가능성이 낮다.
표본 수가 많고, 효과 크기가 크고, 데이터 변동성이 작을수록 검정력이 증가하는 경향이 있다.

---

### 문제 25. 신뢰구간 해석

어떤 알고리즘의 평균 처리시간 감소량에 대한 95% 신뢰구간이 다음과 같이 나왔다.

$$
[2.1, 5.4]
$$

감소량이 0인지에 대한 귀무가설을 유의수준 5%에서 검정할 때 적절한 판단은 무엇인가?

① 0이 신뢰구간에 포함되지 않으므로 감소 효과가 통계적으로 유의하다고 볼 수 있다.
② 0이 신뢰구간에 포함되지 않으므로 감소 효과가 전혀 없다고 본다.
③ 신뢰구간이 양수이므로 검정은 불가능하다.
④ 신뢰구간은 가설검정과 아무 관련이 없다.

#### 정답

①

#### 해설

95% 신뢰구간과 유의수준 5%의 양측 검정은 서로 연결된다.
감소량 0이 신뢰구간 안에 포함되지 않으므로, 감소 효과가 0이라는 귀무가설을 유의수준 5%에서 기각할 수 있다.

---

### 문제 26. z-검정 계산

어떤 장비의 평균 측정값이 기존에는 100이라고 알려져 있다.
새 장비 설정 후 표본 64개를 측정했더니 표본평균은 104, 모집단 표준편차는 16이라고 하자.
귀무가설에서 평균이 100이라고 할 때 z 통계량은 얼마인가?

① 1
② 2
③ 3
④ 4

#### 정답

②

#### 해설

z 통계량은 다음과 같다.

$$
Z = \frac{\bar{x} - \mu_0}{\sigma / \sqrt{n}}
$$

값을 대입하면 다음과 같다.

$$
Z = \frac{104 - 100}{16 / \sqrt{64}}
$$

$$
Z = \frac{4}{16 / 8} = \frac{4}{2} = 2
$$

따라서 z 통계량은 2이다.

---

## 7. 회귀 분석

### 문제 27. 회귀의 목적

다음 중 회귀(Regression) 문제로 가장 적절한 것은 무엇인가?

① 이메일이 스팸인지 정상인지 분류한다.
② 고객을 비슷한 그룹으로 묶는다.
③ 광고비를 이용해 매출 금액을 예측한다.
④ 장바구니에서 함께 구매되는 상품 조합을 찾는다.

#### 정답

③

#### 해설

회귀는 하나 이상의 독립변수를 사용해 연속형 종속변수를 설명하거나 예측하는 방법이다.
매출 금액은 연속값이므로 광고비를 이용해 매출을 예측하는 것은 회귀 문제이다.
스팸 여부는 분류, 고객 그룹 묶기는 Clustering, 상품 조합 탐색은 Association Rule에 가깝다.

---

### 문제 28. 다중선형회귀 계수 해석

다중선형회귀(Multiple Linear Regression)에서 광고비 계수가 3이라고 하자.
다른 변수를 고정했을 때 광고비가 1단위 증가하면 매출은 평균적으로 어떻게 변하는가?

① 3단위 증가한다.
② 3단위 감소한다.
③ 변하지 않는다.
④ 반드시 3배가 된다.

#### 정답

①

#### 해설

다중선형회귀에서 회귀계수는 다른 변수들을 고정했을 때 해당 독립변수가 1단위 증가하면 종속변수가 평균적으로 얼마나 변하는지를 나타낸다.
광고비 계수가 3이면 광고비가 1단위 증가할 때 매출은 평균적으로 3단위 증가한다고 해석한다.

---

### 문제 29. 등분산성

회귀모델의 잔차를 확인했더니 예측값이 커질수록 잔차의 퍼짐도 점점 커졌다.
이때 의심할 수 있는 문제는 무엇인가?

① 등분산성 위반
② 독립변수 없음
③ 종속변수 없음
④ 완전한 정규성 만족

#### 정답

①

#### 해설

등분산성(Homoscedasticity)은 독립변수 값이나 예측값이 달라져도 오차의 분산이 일정해야 한다는 가정이다.
예측값이 커질수록 잔차의 퍼짐이 커진다면 오차 분산이 일정하지 않으므로 등분산성 위반을 의심할 수 있다.
등분산성이 깨지면 회귀계수의 표준오차, 신뢰구간, p-value가 왜곡될 수 있다.

---

### 문제 30. 로지스틱 회귀

다음 중 로지스틱 회귀(Logistic Regression)를 적용하기 가장 적절한 문제는 무엇인가?

① 내일의 평균 기온 예측
② 주택 가격 예측
③ 질병 있음 또는 없음 예측
④ 제품 수명 시간 예측

#### 정답

③

#### 해설

로지스틱 회귀는 종속변수가 이진 분류(Binary Classification)일 때 사용하는 모델이다.
질병 있음 또는 없음은 이진 분류 문제이다.
기온, 주택 가격, 제품 수명은 연속값 예측이므로 일반적인 회귀 문제에 가깝다.

---

### 문제 31. Odds 계산

어떤 사건이 발생할 확률이 0.8이다.
이 사건의 Odds는 얼마인가?

① 0.25
② 0.8
③ 1.25
④ 4

#### 정답

④

#### 해설

Odds는 어떤 사건이 일어날 확률과 일어나지 않을 확률의 비율이다.

$$
Odds = \frac{p}{1 - p}
$$

값을 대입하면 다음과 같다.

$$
Odds = \frac{0.8}{1 - 0.8} = \frac{0.8}{0.2} = 4
$$

따라서 정답은 4이다.

---

### 문제 32. Ridge와 Lasso

다음 중 Lasso Regression에 대한 설명으로 가장 적절한 것은 무엇인가?

① L2 Regularization을 사용하며 계수를 0으로 만들지 않는다.
② L1 Regularization을 사용하며 일부 변수의 계수를 0으로 만들 수 있다.
③ Decision Tree의 가지치기 방법이다.
④ 이진 분류에서만 사용할 수 있다.

#### 정답

②

#### 해설

Lasso Regression은 L1 Regularization을 사용하는 회귀이다.
중요하지 않은 변수의 계수를 0으로 만들 수 있어 변수 선택 효과가 있다.
Ridge Regression은 L2 Regularization을 사용하며 계수 크기를 줄이지만 보통 0으로 만들지는 않는다.

---

### 문제 33. R-squared와 Adjusted R-squared

다음 중 Adjusted R-squared에 대한 설명으로 올바른 것은 무엇인가?

① 변수를 추가하면 항상 증가한다.
② 변수 수를 고려해 보정한 결정계수이다.
③ 분류 모델의 정확도이다.
④ 회귀모델의 오차 제곱 평균이다.

#### 정답

②

#### 해설

R-squared는 종속변수 변동 중 모델이 설명하는 비율이다.
하지만 R-squared는 변수를 추가하면 보통 증가하거나 유지된다.
Adjusted R-squared는 변수 수를 고려해 보정한 값이므로 불필요한 변수를 추가하면 감소할 수 있다.
설명변수 수가 다른 회귀 모델을 비교할 때 더 적절하다.

---

### 문제 34. 다중공선성

회귀모델에서 VIF(Variance Inflation Factor)가 12인 변수가 발견되었다.
가장 적절한 해석은 무엇인가?

① 공선성이 거의 없다.
② 강한 다중공선성 가능성이 있다.
③ 종속변수가 이산형이다.
④ 모델이 반드시 완벽하다.

#### 정답

②

#### 해설

VIF(Variance Inflation Factor)는 다중공선성 때문에 회귀계수 분산이 얼마나 커졌는지 보는 지표이다.
일반적으로 VIF가 5 이상이면 공선성을 의심하고, 10 이상이면 강한 공선성 가능성이 있다고 본다.
VIF가 12이면 해당 변수가 다른 독립변수들과 강하게 중복될 가능성이 있다.

---

## 8. 머신러닝 학습 방식과 Decision Tree

### 문제 35. 지도학습과 비지도학습

다음 중 비지도학습(Unsupervised Learning)에 해당하는 것은 무엇인가?

① Label이 있는 데이터로 스팸 여부를 예측한다.
② Label 없이 고객 행동 패턴을 묶어 군집을 찾는다.
③ 정답이 있는 이미지로 고양이와 강아지를 분류한다.
④ 과거 매출과 광고비로 미래 매출을 예측한다.

#### 정답

②

#### 해설

비지도학습은 정답 Label 없이 데이터 구조나 패턴을 찾는 방식이다.
Label 없이 고객 행동 패턴을 군집화하는 것은 비지도학습이다.
스팸 분류, 이미지 분류, 매출 예측은 정답 Label 또는 목표값이 있는 지도학습에 해당한다.

---

### 문제 36. Decision Tree의 불순도

Decision Tree에서 불순도(Impurity)가 낮다는 것은 무엇을 의미하는가?

① 노드 안에 여러 클래스가 반반 섞여 있다.
② 노드 안의 데이터가 한 클래스에 가깝게 모여 있다.
③ 모델이 반드시 과적합되었다.
④ Feature가 하나도 없다.

#### 정답

②

#### 해설

불순도는 한 노드 안에 여러 클래스가 얼마나 섞여 있는지를 나타낸다.
한 클래스만 있거나 한 클래스 비율이 매우 높으면 불순도가 낮다.
Decision Tree는 분기 후 자식 노드의 불순도가 낮아지는 방향으로 Feature를 선택한다.

---

### 문제 37. Gini Impurity 계산

어떤 노드에 양성 6개, 음성 4개가 있다.
이진 분류에서 Gini Impurity를 구하라.

① 0.24
② 0.48
③ 0.60
④ 1.00

#### 정답

②

#### 해설

Gini Impurity는 다음과 같이 계산한다.

$$
Gini = 1 - \sum p_i^2
$$

양성 비율은 0.6, 음성 비율은 0.4이다.

$$
Gini = 1 - (0.6^2 + 0.4^2)
$$

$$
Gini = 1 - (0.36 + 0.16) = 0.48
$$

따라서 정답은 0.48이다.

---

### 문제 38. Information Gain

부모 노드의 Entropy가 0.9이고, 어떤 Feature로 분기한 뒤 자식 노드들의 가중 평균 Entropy가 0.4이다.
Information Gain은 얼마인가?

① 0.3
② 0.4
③ 0.5
④ 1.3

#### 정답

③

#### 해설

Information Gain은 분기 전 Entropy에서 분기 후 가중 평균 Entropy를 뺀 값이다.

$$
Information\ Gain = 0.9 - 0.4 = 0.5
$$

Information Gain이 클수록 해당 분기가 불확실성을 많이 줄였다는 뜻이다.

---

### 문제 39. Gain Ratio

Information Gain이 ID처럼 값 종류가 많은 Feature를 과도하게 선호할 수 있다.
이 문제를 완화하기 위해 사용하는 지표는 무엇인가?

① Gain Ratio
② Accuracy
③ RMSE(Root Mean Squared Error)
④ Odds

#### 정답

①

#### 해설

Gain Ratio는 Information Gain을 Split Information으로 나누어 보정한 지표이다.
ID처럼 거의 모든 값이 다른 Feature는 데이터를 잘게 나누면서 Information Gain을 크게 만들 수 있다.
하지만 이런 분기는 일반화에 도움이 되지 않을 수 있다. Gain Ratio는 이런 편향을 완화하기 위해 사용된다.

---

### 문제 40. Pruning

Decision Tree에서 Pruning의 목적은 무엇인가?

① 트리를 더 깊고 복잡하게 만들어 학습 데이터에 완벽히 맞추는 것
② 트리의 가지를 조정해 과적합을 줄이는 것
③ 모든 Feature를 삭제하는 것
④ Entropy를 계산하지 않는 것

#### 정답

②

#### 해설

Pruning은 트리를 가지치기해서 과적합을 줄이는 방법이다.
Pre-pruning은 트리가 너무 깊어지기 전에 성장을 제한하고, Post-pruning은 먼저 큰 트리를 만든 뒤 불필요한 가지를 제거한다.
너무 많이 가지치기하면 과소적합이 생길 수 있다.

---

## 9. Bayes 정리와 Clustering

### 문제 41. Bayes 정리의 구성요소

Bayes 정리에서 사후확률(Posterior Probability)은 무엇인가?

① 증거를 보기 전 원인의 확률
② 원인이 참일 때 증거가 나올 확률
③ 증거를 본 뒤 원인이 참일 확률
④ 전체 transaction 중 itemset이 등장한 비율

#### 정답

③

#### 해설

Bayes 정리는 증거가 관측되었을 때 원인의 확률을 갱신하는 방법이다.
사전확률(Prior Probability)은 증거를 보기 전 확률이고, 사후확률(Posterior Probability)은 증거를 본 뒤 원인이 참일 확률이다.

---

### 문제 42. Bayes 정리 계산

어떤 질병의 유병률은 1%이다.
질병이 있을 때 검사 양성 확률은 99%이고, 질병이 없을 때 위양성 확률은 5%이다.
검사 결과가 양성일 때 실제 질병이 있을 확률에 가장 가까운 값은 무엇인가?

① 약 1.0%
② 약 16.7%
③ 약 50.0%
④ 약 99.0%

#### 정답

②

#### 해설

임의로 10,000명을 기준으로 생각하면 쉽다.
질병이 있는 사람은 1%이므로 100명이다.
이 중 양성은 99명이 된다.
질병이 없는 사람은 9,900명이다.
이 중 위양성은 다음과 같다.

$$
9900 \times 0.05 = 495
$$

전체 양성은 다음과 같다.

$$
99 + 495 = 594
$$

양성 중 실제 질병이 있는 비율은 다음과 같다.

$$
\frac{99}{594} \approx 0.167
$$

따라서 약 16.7%이다.
검사의 민감도가 99%여도 유병률이 낮고 위양성이 있으면 양성 결과의 실제 질병 확률은 크게 낮아질 수 있다.

---

### 문제 43. Naive Bayes

Naive Bayes 분류 모델의 핵심 가정은 무엇인가?

① Feature들이 서로 조건부 독립이라고 가정한다.
② 모든 Feature가 반드시 연속형이라고 가정한다.
③ 데이터가 반드시 선형 관계라고 가정한다.
④ Label이 없어야 한다고 가정한다.

#### 정답

①

#### 해설

Naive Bayes는 Feature들이 서로 조건부 독립이라고 가정하는 분류 모델이다.
이 가정은 강하지만, 텍스트 분류와 스팸 필터에서 빠르고 실용적인 성능을 보이는 경우가 많다.

---

### 문제 44. K-means Clustering

K-means Clustering의 일반적인 동작 순서로 가장 적절한 것은 무엇인가?

① Label 입력, 회귀계수 계산, p-value 판단
② centroid 초기화, 가까운 centroid에 할당, cluster 평균으로 centroid 갱신, 반복
③ Entropy 계산, Gain Ratio 계산, 가지치기
④ 사전확률 계산, 가능도 계산, 사후확률 계산

#### 정답

②

#### 해설

K-means Clustering은 데이터를 K개의 cluster로 나누는 대표적인 비지도학습 알고리즘이다.
일반적인 순서는 K개의 centroid를 초기화하고, 각 데이터를 가장 가까운 centroid에 할당한 뒤, 각 cluster의 평균으로 centroid를 갱신하고 이를 반복하는 방식이다.

---

### 문제 45. DBSCAN

다음 중 DBSCAN(Density-Based Spatial Clustering of Applications with Noise)의 장점으로 가장 적절한 것은 무엇인가?

① K를 반드시 미리 정해야 한다.
② 밀도가 낮은 점을 noise로 처리할 수 있다.
③ 모든 cluster가 반드시 구형이어야 한다.
④ Label이 있어야만 학습할 수 있다.

#### 정답

②

#### 해설

DBSCAN은 밀도 기반 Clustering 알고리즘이다.
밀도가 높은 영역을 cluster로 보고, 밀도가 낮은 점은 noise로 처리한다.
K-means와 달리 K를 미리 정하지 않아도 되며, 복잡한 모양의 cluster를 찾는 데 유리할 수 있다.

---

## 10. 차원축소와 Feature Selection

### 문제 46. PCA

PCA(Principal Component Analysis, 주성분분석)에 대한 설명으로 가장 적절한 것은 무엇인가?

① Target과의 상관이 가장 높은 변수만 선택한다.
② 변수들의 분산을 최대한 보존하는 새로운 축을 찾아 차원을 줄인다.
③ 범주형 변수를 숫자로 변환한다.
④ 결측치를 평균으로 대체한다.

#### 정답

②

#### 해설

PCA(Principal Component Analysis)는 변수들의 분산을 최대한 보존하는 새로운 축을 찾아 데이터를 저차원으로 변환하는 방법이다.
다중공선성 완화, 시각화, 계산량 감소에 유용하다.
단, 주성분은 원래 변수들의 조합이므로 해석이 어려울 수 있고, 스케일링이 필요하다.

---

### 문제 47. Feature Selection 방식

모델 학습 과정에서 변수 선택이 함께 이루어지는 방식은 무엇인가?

① Filter Method
② Wrapper Method
③ Embedded Method
④ Min-Max Scaling

#### 정답

③

#### 해설

Feature Selection 방식은 크게 Filter Method, Wrapper Method, Embedded Method로 나눌 수 있다.
Embedded Method는 모델 학습 과정에서 변수 선택이 함께 이루어진다.
예를 들어 Lasso Regression처럼 학습 중 일부 계수를 0으로 만드는 모델이 이에 해당한다.

---

## 11. Association Rule

### 문제 48. Support 계산

전체 transaction이 100개이다.
그중 우유와 빵이 함께 포함된 transaction이 20개이다.
규칙 우유 -> 빵의 Support는 얼마인가?

① 0.1
② 0.2
③ 0.5
④ 2.0

#### 정답

②

#### 해설

Support는 특정 itemset이 전체 transaction 중 얼마나 자주 등장하는지 나타낸다.

$$
Support = \frac{\text{우유와 빵이 모두 포함된 transaction 수}}{\text{전체 transaction 수}}
$$

$$
Support = \frac{20}{100} = 0.2
$$

Support는 A -> B와 B -> A에서 같다.

---

### 문제 49. Confidence 계산

전체 transaction 중 우유가 포함된 transaction은 40개이고, 우유와 빵이 함께 포함된 transaction은 20개이다.
규칙 우유 -> 빵의 Confidence는 얼마인가?

① 0.2
② 0.4
③ 0.5
④ 2.0

#### 정답

③

#### 해설

Confidence는 A가 등장한 transaction 중 B도 등장한 비율이다.

$$
Confidence(\text{우유 -> 빵}) = \frac{\text{우유와 빵이 모두 포함된 transaction 수}}{\text{우유가 포함된 transaction 수}}
$$

$$
Confidence = \frac{20}{40} = 0.5
$$

따라서 우유가 등장한 transaction 중 50%에서 빵도 함께 등장했다는 뜻이다.

---

### 문제 50. Lift 해석

어떤 규칙 A -> B의 Lift가 1.8이다.
가장 적절한 해석은 무엇인가?

① A와 B는 독립에 가깝다.
② A가 있을 때 B가 오히려 덜 나타난다.
③ A와 B가 함께 나타나는 경향이 독립일 때보다 강하다.
④ A와 B는 절대 함께 나타나지 않는다.

#### 정답

③

#### 해설

Lift는 A와 B가 독립적으로 등장하는 경우보다 얼마나 더 강하게 함께 나타나는지 나타낸다.
Lift가 1보다 크면 A와 B가 함께 나타나는 경향이 강하다는 뜻이다.
Lift가 1이면 독립에 가깝고, 1보다 작으면 A가 있을 때 B가 덜 나타나는 경향이 있다.

---

## 12. 모델 평가 지표

### 문제 51. 혼동행렬 용어

실제는 양성인데 모델이 음성으로 예측했다.
이 경우는 무엇인가?

① TP(True Positive, 진양성)
② TN(True Negative, 진음성)
③ FP(False Positive, 위양성)
④ FN(False Negative, 위음성)

#### 정답

④

#### 해설

FN(False Negative, 위음성)은 실제 양성을 음성으로 잘못 예측한 경우이다.
질병 진단, 불량 탐지, 장애 탐지처럼 양성을 놓치면 위험한 문제에서는 FN을 줄이는 것이 중요하다.

---

### 문제 52. Accuracy 계산

분류 모델의 결과가 다음과 같다.

| 구분 | 개수 |
|---|---:|
| TP(True Positive, 진양성) | 30 |
| TN(True Negative, 진음성) | 50 |
| FP(False Positive, 위양성) | 10 |
| FN(False Negative, 위음성) | 10 |

Accuracy는 얼마인가?

① 0.3
② 0.5
③ 0.8
④ 0.9

#### 정답

③

#### 해설

Accuracy는 전체 데이터 중 맞게 예측한 비율이다.

$$
Accuracy = \frac{TP + TN}{TP + TN + FP + FN}
$$

값을 대입하면 다음과 같다.

$$
Accuracy = \frac{30 + 50}{30 + 50 + 10 + 10} = \frac{80}{100} = 0.8
$$

---

### 문제 53. Precision 계산

분류 모델의 결과가 다음과 같다.

| 구분 | 개수 |
|---|---:|
| TP(True Positive, 진양성) | 40 |
| FP(False Positive, 위양성) | 10 |
| FN(False Negative, 위음성) | 20 |
| TN(True Negative, 진음성) | 30 |

Precision은 얼마인가?

① 0.50
② 0.67
③ 0.80
④ 0.90

#### 정답

③

#### 해설

Precision은 모델이 양성이라고 예측한 것 중 실제 양성의 비율이다.

$$
Precision = \frac{TP}{TP + FP}
$$

값을 대입하면 다음과 같다.

$$
Precision = \frac{40}{40 + 10} = \frac{40}{50} = 0.8
$$

Precision은 위양성(False Positive)을 줄이는 것이 중요한 문제에서 유용하다.

---

### 문제 54. Recall 계산

분류 모델의 결과가 다음과 같다.

| 구분 | 개수 |
|---|---:|
| TP(True Positive, 진양성) | 40 |
| FP(False Positive, 위양성) | 10 |
| FN(False Negative, 위음성) | 20 |
| TN(True Negative, 진음성) | 30 |

Recall은 얼마인가?

① 0.50
② 0.67
③ 0.80
④ 0.90

#### 정답

②

#### 해설

Recall은 실제 양성 중 모델이 양성으로 맞춘 비율이다.
민감도(Sensitivity)와 같은 의미로 사용된다.

$$
Recall = \frac{TP}{TP + FN}
$$

값을 대입하면 다음과 같다.

$$
Recall = \frac{40}{40 + 20} = \frac{40}{60} \approx 0.67
$$

Recall은 실제 양성을 놓치면 위험한 의료, 불량 탐지, 장애 탐지 문제에서 중요하다.

---

### 문제 55. Specificity 계산

분류 모델의 결과가 다음과 같다.

| 구분 | 개수 |
|---|---:|
| TP(True Positive, 진양성) | 40 |
| FP(False Positive, 위양성) | 10 |
| FN(False Negative, 위음성) | 20 |
| TN(True Negative, 진음성) | 30 |

Specificity는 얼마인가?

① 0.50
② 0.67
③ 0.75
④ 0.80

#### 정답

③

#### 해설

Specificity는 실제 음성 중 모델이 음성으로 맞춘 비율이다.

$$
Specificity = \frac{TN}{TN + FP}
$$

값을 대입하면 다음과 같다.

$$
Specificity = \frac{30}{30 + 10} = \frac{30}{40} = 0.75
$$

Specificity는 실제 음성을 잘 보호해야 하는 문제에서 중요하다.

---

### 문제 56. F1-score 계산

Precision이 0.8이고 Recall이 0.5이다.
F1-score는 얼마인가?

① 약 0.50
② 약 0.62
③ 약 0.80
④ 약 1.30

#### 정답

②

#### 해설

F1-score는 Precision과 Recall의 조화평균이다.

$$
F1 = \frac{2 \times Precision \times Recall}{Precision + Recall}
$$

값을 대입하면 다음과 같다.

$$
F1 = \frac{2 \times 0.8 \times 0.5}{0.8 + 0.5}
$$

$$
F1 = \frac{0.8}{1.3} \approx 0.62
$$

F1-score는 Precision과 Recall의 균형을 보고 싶을 때 유용하다.

---

### 문제 57. ROC Curve와 PR Curve

양성 클래스가 매우 희귀한 불균형 데이터에서 양성 예측 품질을 더 민감하게 보고 싶다.
어떤 평가 그래프가 더 적절한 경우가 많은가?

① ROC Curve
② PR Curve(Precision-Recall Curve)
③ Q-Q Plot(Quantile-Quantile Plot)
④ Box Plot

#### 정답

②

#### 해설

ROC Curve는 TPR(True Positive Rate)과 FPR(False Positive Rate)의 관계를 본다.
음성 데이터가 매우 많은 불균형 상황에서는 ROC Curve가 좋아 보일 수 있다.
PR Curve는 Precision과 Recall의 관계를 보기 때문에 희귀 양성 탐지 문제에서 양성 예측 품질을 더 민감하게 보여주는 경우가 많다.

---

### 문제 58. 회귀 평가 지표

큰 예측 오차에 더 강한 패널티를 주고 싶고, 최종 지표는 원래 단위로 해석하고 싶다.
가장 적절한 회귀 평가 지표는 무엇인가?

① MAE(Mean Absolute Error)
② MSE(Mean Squared Error)
③ RMSE(Root Mean Squared Error)
④ Accuracy

#### 정답

③

#### 해설

MSE(Mean Squared Error)는 오차를 제곱하므로 큰 오차에 강한 패널티를 준다. 하지만 단위가 제곱이라 해석이 어렵다.
RMSE(Root Mean Squared Error)는 MSE에 제곱근을 취해 원래 단위로 해석할 수 있다.
따라서 큰 오차를 중요하게 반영하면서 원래 단위 해석도 필요한 경우 RMSE가 적절하다.

---

## 13. 데이터 전처리와 검증

### 문제 59. 결측치 처리

다음 중 결측치(Missing Value)를 처리할 때 가장 먼저 고려해야 할 점은 무엇인가?

① 무조건 모든 결측 행을 삭제한다.
② 결측이 단순 누락인지, 특정 의미를 가지는지 확인한다.
③ 결측값을 항상 0으로 채운다.
④ 결측치가 있으면 모델링이 반드시 불가능하다.

#### 정답

②

#### 해설

결측치 처리는 데이터 분석에서 중요하다.
결측이 단순히 누락된 것인지, 특정 상황에서만 발생하는 의미 있는 신호인지 먼저 확인해야 한다.
무조건 삭제하거나 0으로 대체하면 데이터 손실이나 왜곡이 발생할 수 있다.

---

### 문제 60. 이상치 처리

이상치(Outlier)에 대한 올바른 태도는 무엇인가?

① 이상치는 항상 오류이므로 무조건 제거한다.
② 이상치는 실제 중요한 희귀 현상일 수도 있으므로 도메인 의미를 먼저 확인한다.
③ 이상치는 평균 계산에 전혀 영향을 주지 않는다.
④ 이상치는 분류 모델에서만 발생한다.

#### 정답

②

#### 해설

이상치는 대부분 데이터와 크게 다른 관측값이다.
오류일 수도 있지만, 실제 중요한 희귀 현상일 수도 있다.
예를 들어 통신 장애 로그에서 극단적인 값은 단순 오류가 아니라 실제 장애 패턴일 수 있다.
따라서 무조건 제거하기 전에 도메인 의미를 먼저 확인해야 한다.

---

### 문제 61. Encoding 방식

순서가 없는 범주형 변수에 Label Encoding을 적용할 때 발생할 수 있는 문제는 무엇인가?

① 범주 수가 줄어든다.
② 순서가 없는 범주에 가짜 순서가 생길 수 있다.
③ 결측치가 모두 사라진다.
④ 모델이 반드시 과소적합된다.

#### 정답

②

#### 해설

Label Encoding은 범주를 정수로 변환하는 방식이다.
순서가 없는 범주에 0, 1, 2 같은 숫자를 부여하면 모델이 잘못된 순서 관계로 해석할 수 있다.
순서 왜곡을 피하려면 One-hot Encoding을 사용할 수 있다.

---

### 문제 62. 데이터 누수

다음 중 데이터 누수(Data Leakage)에 해당하는 것은 무엇인가?

① train/test를 먼저 나눈 뒤 train 기준으로 스케일링한다.
② 테스트 데이터 전체 정보를 사용해 스케일링 기준을 만든 뒤 모델을 학습한다.
③ Cross Validation의 각 fold 안에서 전처리를 따로 fit한다.
④ 예측 시점 이후 정보를 Feature에서 제거한다.

#### 정답

②

#### 해설

데이터 누수는 학습 시점에 알 수 없어야 하는 정보가 모델 학습에 들어가는 문제이다.
테스트 데이터 전체를 기준으로 스케일링한 뒤 학습하면 테스트 정보가 전처리 과정에 반영된다.
따라서 train/test를 먼저 분리하고, 전처리 기준은 train에서만 학습해야 한다.

---

### 문제 63. 과적합과 과소적합

학습 데이터 성능은 매우 좋지만, 새로운 데이터 성능이 나쁘다.
이 상태를 무엇이라고 하는가?

① 과적합(Overfitting)
② 과소적합(Underfitting)
③ 표준화(Standardization)
④ 독립성(Independence)

#### 정답

①

#### 해설

과적합(Overfitting)은 모델이 학습 데이터에 지나치게 맞춰져 새로운 데이터에서 성능이 떨어지는 상태이다.
모델이 너무 복잡하거나, 데이터가 적거나, 노이즈까지 패턴으로 학습했을 때 발생할 수 있다.
Regularization, Cross Validation, Pruning, Early Stopping, 데이터 증가 등으로 완화할 수 있다.

---

### 문제 64. Cross Validation

Cross Validation에 대한 설명으로 가장 적절한 것은 무엇인가?

① 데이터를 여러 fold로 나누어 학습과 검증을 반복하는 방법이다.
② 테스트 데이터를 학습에 직접 사용하는 방법이다.
③ 결측치를 모두 삭제하는 전처리 방법이다.
④ 모든 Feature를 하나로 합치는 방법이다.

#### 정답

①

#### 해설

Cross Validation은 데이터를 여러 fold로 나누어 학습과 검증을 반복하는 방법이다.
성능 평가를 더 안정적으로 만들고 데이터를 효율적으로 사용할 수 있다.
다만 계산 비용이 증가하며, 시간 순서가 중요한 데이터에서는 랜덤 분할을 주의해야 한다.

---

### 문제 65. Q-Q Plot 해석

Q-Q Plot(Quantile-Quantile Plot)에서 점들이 대체로 직선에 가깝게 놓여 있다.
가장 적절한 해석은 무엇인가?

① 데이터가 비교 대상 분포에 가깝다.
② 데이터가 반드시 범주형이다.
③ 데이터에 결측치가 없다.
④ 모델의 Accuracy가 높다.

#### 정답

①

#### 해설

Q-Q Plot은 데이터의 분위수와 이론 분포의 분위수를 비교하는 그래프이다.
점들이 직선에 가까우면 데이터가 비교 대상 분포에 가깝다고 해석한다.
양쪽 끝이 크게 벗어나면 꼬리가 두껍거나 이상치 가능성이 있고, S자 형태는 왜도나 꼬리 차이를 의심할 수 있다.

---

## 14. 종합 문제

### 문제 66. 문제 유형과 모델 선택

RF 로그에서 RSRP(Reference Signal Received Power), RSRQ(Reference Signal Received Quality), SINR(Signal to Interference plus Noise Ratio)를 사용해 Call Drop 발생 여부를 예측하려고 한다.
이 문제 유형과 시작 모델로 가장 적절한 것은 무엇인가?

① 연속값 예측, Linear Regression
② 이진 분류, Logistic Regression 또는 Decision Tree
③ 군집화, K-means Clustering
④ 장바구니 분석, Apriori Algorithm

#### 정답

②

#### 해설

Call Drop 발생 여부는 발생 또는 미발생의 이진 Label이다.
따라서 이진 분류 문제이다.
시작 모델로는 Logistic Regression, Decision Tree, Random Forest 등을 비교할 수 있다.

---

### 문제 67. 평가 지표 선택

불량 단말을 놓치면 큰 비용이 발생한다.
즉 실제 불량인데 정상으로 예측하는 FN(False Negative, 위음성)을 줄이는 것이 가장 중요하다.
가장 중점적으로 볼 지표는 무엇인가?

① Recall 또는 Sensitivity
② Precision
③ Specificity
④ R-squared

#### 정답

①

#### 해설

Recall은 실제 양성 중 모델이 양성으로 맞춘 비율이다.
FN(False Negative, 위음성)을 줄이는 것이 중요하면 Recall 또는 Sensitivity를 중점적으로 본다.
불량 탐지, 질병 진단, 장애 탐지에서 특히 중요하다.

---

### 문제 68. 지표 해석 종합

어떤 모델은 Accuracy가 99%이지만 실제 양성 데이터의 대부분을 놓친다.
데이터는 음성이 99%, 양성이 1%이다.
이 상황에서 Accuracy만 보면 위험한 이유는 무엇인가?

① Accuracy는 항상 0이기 때문이다.
② 클래스 불균형에서 다수 클래스만 잘 맞춰도 Accuracy가 높게 나올 수 있기 때문이다.
③ Accuracy는 회귀 모델에만 사용하는 지표이기 때문이다.
④ Accuracy는 Precision과 항상 같은 값이기 때문이다.

#### 정답

②

#### 해설

클래스 불균형 데이터에서는 모든 데이터를 음성으로 예측해도 Accuracy가 매우 높게 나올 수 있다.
하지만 실제 양성을 대부분 놓치면 실무적으로 매우 위험할 수 있다.
따라서 Recall, Precision, F1-score, PR Curve 등을 함께 확인해야 한다.

---

### 문제 69. 전처리와 모델 선택 종합

KNN(K-Nearest Neighbors), K-means, SVM(Support Vector Machine)처럼 거리 기반 모델을 사용하려고 한다.
Feature마다 단위와 범위가 크게 다르다.
가장 먼저 고려할 전처리는 무엇인가?

① Standardization 또는 Normalization
② Target Encoding만 적용
③ 모든 Feature 삭제
④ Label을 제거

#### 정답

①

#### 해설

거리 기반 모델은 변수의 스케일 차이에 민감하다.
예를 들어 한 Feature는 0에서 1 사이이고 다른 Feature는 0에서 100,000 사이이면 거리 계산이 큰 범위의 Feature에 지배될 수 있다.
따라서 Standardization 또는 Normalization을 고려해야 한다.

---

### 문제 70. 통계와 머신러닝 흐름

다음 중 데이터 사이언스의 일반적인 흐름으로 가장 자연스러운 것은 무엇인가?

① 모델링, 데이터 수집, 문제 정의, 평가
② 문제 정의, 데이터 수집, 데이터 이해와 전처리, 모델링, 평가와 활용
③ 평가, 활용, 문제 정의, 데이터 삭제
④ 전처리 없이 바로 최종 모델 배포

#### 정답

②

#### 해설

데이터 사이언스는 보통 문제 정의에서 시작한다.
이후 데이터 수집, 데이터 이해, 전처리, 모델링, 평가, 활용 순서로 진행된다.
문제를 명확히 정의하지 않으면 어떤 데이터를 모아야 하는지, 어떤 평가 지표를 써야 하는지 판단하기 어렵다.
